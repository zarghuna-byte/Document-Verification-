# Base URL of the FastAPI backend. Consumed by the axios instance in
# src/services/api.js. No API calls are made in this phase; this file exists so
# future phases can point the client at the correct server. Copy to `.env.local`
# and adjust per environment.
VITE_API_BASE_URL=http://localhost:8000/api/v1
